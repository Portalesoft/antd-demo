import React from 'react';
import { Button } from 'antd';

import './Support.css';

const support = (props) => (
    <div className="Support">
        <Button type="primary" icon="question-circle-o" size="large">Help</Button>
    </div>
);

export default support;